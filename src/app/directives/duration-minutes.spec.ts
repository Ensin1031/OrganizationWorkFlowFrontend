import { DurationMinutesDirective } from './duration-minutes';

describe('DurationMinutesDirective', () => {
  it('should create an instance', () => {
    const directive = new DurationMinutesDirective();
    expect(directive).toBeTruthy();
  });
});
